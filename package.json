{
  "name": "app",
  "version": "1.0.0",
  "description": "``` docker run -it -v ${pwd}:/app --rm /bin/bash ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.45"
  }
}
